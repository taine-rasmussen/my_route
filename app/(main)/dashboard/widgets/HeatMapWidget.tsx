import { Card, SizableText } from 'tamagui';

const HeatMapWidget = () => {
  return (
    <Card padding={16} elevate size="$5" bordered width="auto" height={'auto'}>
      <SizableText>Placeholder text for heatmap</SizableText>
    </Card>
  );
};

export default HeatMapWidget;
